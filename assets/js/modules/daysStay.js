/*
export function daysStay(dueDate) {
    const dueDateObj = new Date(dueDate);
    const now = new Date();
    const timeStay = dueDateObj.getTime() - now.getTime();
    const daysStay = Math.ceil(timeStay / (1000 * 60 * 60 * 24));
    return daysStay;
  }*/
